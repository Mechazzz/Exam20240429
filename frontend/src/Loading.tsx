const Loading = () => {
  return (
    <div className="text-gray-200 text-lg font-semibold animate-pulse">
      Loading movies<span className="animate-pulse">.</span>
      <span className="animate-pulse">.</span>
      <span className="animate-pulse">.</span>
    </div>
  );
};

export default Loading;
